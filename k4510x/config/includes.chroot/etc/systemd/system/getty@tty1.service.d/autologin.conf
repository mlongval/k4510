# K4510x: tty1 logs the machine's user straight in.  The emulator is started
# from /etc/profile.d/k4510x.sh, not from here, so that QUITTING it leaves a
# working login shell on this tty instead of a respawn loop.
[Service]
ExecStart=
ExecStart=-/sbin/agetty --autologin k4510 --noclear %I $TERM
