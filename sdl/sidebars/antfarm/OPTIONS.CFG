# Ant farm: its options.  This copy is the defaults; edit the one in
# /SYSTEM/SIDEBARS/ANTFARM/ (F12 -> Edit options).

speed = 1          # 0.25 to 4: how fast everything happens
day   = real       # real: the host's clock.  Or a length: 30m, 1h
